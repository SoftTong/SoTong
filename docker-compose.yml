version: '3'

services:
  mysql:
    image: mysql:latest
    restart: always
    volumes:
      - dbdata:/var/lib/mysql
    ports:
      - 8989:3306
    cap_add:
      - SYS_NICE
    environment: 
      - MYSQL_ROOT_PASSWORD=sotong
      - MYSQL_USER=sotong
      - MYSQL_PASSWORD=sotong

  spring-boot:
  

  react:
    
  
volumes:
  dbdata: